import type { Resource } from './resource';
import { FIELD_TYPE, ResourceData } from './resource.models';
import { SHORT_FIELD_TYPE } from '../search';
interface AlgoliaRecord {
    title?: string;
    fullText?: string[];
    images?: string[];
}
export declare const resourceToAlgoliaFormat: (resource: Resource, backend: string) => AlgoliaRecord;
/**
 * Currently in our models, there are more FIELD_TYPEs than ResourceData keys, so we need the switch for typing reason
 */
export declare const getDataKeyFromFieldType: (fieldType: FIELD_TYPE) => keyof ResourceData | null;
export declare function longToShortFieldType(fieldType: FIELD_TYPE): SHORT_FIELD_TYPE;
export declare function shortToLongFieldType(shortType: SHORT_FIELD_TYPE): FIELD_TYPE | null;
export declare function getFieldTypeFromString(type: string): FIELD_TYPE | null;
export {};
